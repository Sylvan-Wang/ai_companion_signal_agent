# AI Companion Signal Intelligence Agent — Dependencies

# Reddit data collection
praw>=7.7.0

# LLM API
openai>=1.0.0

# Config and environment
pyyaml>=6.0.0
python-dotenv>=1.0.0

# Embedding-based clustering (optional — Phase 2+, sample_size >= 50)
# Uncomment when ready to enable clustering methods:
# sentence-transformers>=2.2.0
# scikit-learn>=1.3.0
# numpy>=1.24.0

# HTTP requests (public JSON Reddit API fallback)
requests>=2.31.0

# Utilities
tqdm>=4.66.0
